import styled from 'styled-components'

const Title = styled.h1`
  font-size: inherit;
  font-weight: 600;
  text-align: center;
  max-height: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  word-wrap: normal;
`

export default Title
